apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helloapi.fullname" . }}-test-connection"
  labels:
    {{- include "helloapi.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "helloapi.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
