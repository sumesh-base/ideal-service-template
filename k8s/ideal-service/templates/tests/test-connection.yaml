apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ideal-service.fullname" . }}-test-connection"
  labels:
    {{- include "ideal-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ideal-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
